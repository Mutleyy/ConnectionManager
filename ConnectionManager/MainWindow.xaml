<Window x:Class="ConnectionManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConnectionManager"
        xmlns:properties="clr-namespace:ConnectionManager.Properties"
        mc:Ignorable="d"
        Title="Connection Manager" SizeToContent="Manual"
        Background="#FF333333"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        MinWidth="700" MinHeight="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="2">
            <Button Content="{StaticResource DoubleIconChevronUp}" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="200" Height="72">
            
            </Button>
            <Button Content="Button" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Height="72"/>
        </StackPanel>
    </Grid>
</Window>
